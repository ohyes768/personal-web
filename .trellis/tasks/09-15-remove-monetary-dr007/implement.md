# 实施计划

1. 先写构建信号的失败测试，断言 DR007 不出现在输出且 MLF/LPR 使用 50/50 权重。
2. 修改构建器和入口，删除 DR007 抓取模块。
3. 更新 SKILL.md，运行目标及全量测试。
## 日频 skill 上传移除

1. 为两个 `run_all.py --help` 写失败测试，断言不显示 `--upload`。
2. 删除两个入口的参数、上传分支及仅用于上传的导入。
3. 从两份 SKILL.md 删除 `--upload` 和线上推送说明；保留日频本地抓取说明。
4. 运行两个 CLI 测试与原货币政策回归测试。