# pnpm 8+ 标准写法：展平所有依赖到 node_modules/，避免 .pnpm/ 软链
# Windows + Next.js standalone 场景下 .pnpm/ 软链会触发 EPERM
node-linker=hoisted

# 国内镜像（本地 dev + Docker build 都生效）
registry=https://registry.npmmirror.com/
