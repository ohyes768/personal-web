# PENDING — 待办与改进项

记录尚未实现的设计想法、改进项，关联 commit 与上下文，便于后续拾起。

---

## 1. fhps 董事会预案加"预估"提示

**背景**（commit `b61cd52` 之后）：
- `src/data/fhps_fetcher.py:22` 已把 `"董事会决议通过"` 加入 `ACCEPTED_PROGRESS_STATUSES`
- 采纳董事会预案会引入**金额失真风险**（股东大会可能改金额/否决），但**用户层面无任何标识**能区分"实施分配"和"董事会预案"

**需求方向**：
- 前端表格 / 卡片 / 报告里，给"董事会决议通过"状态的记录加角标或 tooltip
- 提示文案类似：`"[预估] 董事会预案，股东大会可能修改金额或否决 — 2025-04-21 公告"`
- 数据来源：`fhps_record["方案进度"]` + `fhps_record["预案公告日"]`（calculator.py:360-362 已经写入 `实施方案分红说明`）

**待定细节**：
- 角标位置：表格列、卡片标题、还是 carousel 报告里都加？
- 排序逻辑：是否要让"实施分配"在前，"董事会预案"置底？
- 优先级去重：同一 `(代码, 财年)` 出现多条记录时，是否按 `实施 > 股东大会 > 董事会` 取最新一条？
  - 当前 fetcher 在 `_build_index` 阶段按 `方案进度.isin(...)` 过滤，不做同财年内优先级去重
  - 如果做，cninfo 的"2025年报(实施)" + fhps 的"2025年报(预案)"会**重复进 df**，需要 calculator 段也加去重

**关联文件**：
- `src/data/fhps_fetcher.py`（数据源、过滤规则）
- `src/core/calculator.py`（去重 / 拼接逻辑）
- `apps/dividend/src/...`（前端展示，basePath 空，参考 CLAUDE.md）

---

## 模板（新增待办时复制）

```markdown
## N. <标题>

**背景**：<关联 commit / 触发问题>

**需求方向**：<想法、想达到的效果>

**待定细节**：<open questions>

**关联文件**：<涉及模块>
```
