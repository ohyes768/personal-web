[project]
name = "dividend"
version = "0.2.0"
description = "A股高股息率TOP50查询工具（含Web API）"
requires-python = ">=3.11"
dependencies = [
    "akshare>=1.14.0",
    "pyyaml>=6.0.0",
    "tabulate>=0.9.0",
    # Web API 依赖
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "pydantic>=2.5.0",
    "pandas>=2.0.0",
    "requests>=2.31.0",  # 直接调东财 emweb 接口获取板块信息（替代 efinance）
    "pywencai>=0.13.1",  # 申万行业数据源（问财 API），sw_industry_fetcher.py 使用
    # 环境变量加载
    "python-dotenv>=1.0.0",
    # 定时任务：APScheduler 3.x（不要 4.x，API 不兼容）
    "apscheduler>=3.10,<4.0",
    # scheduler 内部 HTTP self-call：调本地 /api/dividend/* 接口
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["config", "data", "display"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

[tool.coverage.run]
source = ["."]
omit = ["tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
